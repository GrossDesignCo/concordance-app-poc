import { Book } from '@/types';
import numbers_21 from './numbers-21';

export const numbers: Book = {
  meta: {
    name: 'Numbers',
    translationChain:
      'בְּמִדְבַּר (beMidbar/in-Wilderness) → Ἀριθμοί (Arithmoi/Numbers) → Numeri (Numbers) → Numbers',
  },
  chapters: [numbers_21],
};
